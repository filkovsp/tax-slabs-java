package org.example.service;

public interface Calculator {
    /**
     * calculates tax based on the income amount
     * @return calculated tax value
     */
    double calculateTax(double income);
}
